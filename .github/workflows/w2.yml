name: Next

on:
  workflow_run:
    workflows: ["Failure"]
    types: [completed]
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Run
        run: echo "Success!"
